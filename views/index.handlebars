<h1>Burger Builder</h1>

<h2>Pick a Burger to Devour or Delete!</h2>

<ul>
  {{#each burgers}} {{#unless devour}} {{> burgers/burger-block devour=true}} {{/unless}} {{/each}}
</ul>

<h2>Devoured Burgers!</h2>

<ul>
  {{#each burgers}} {{#if devour}} {{> burgers/burger-block devour=false}} {{/if}} {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devour">Choose an Option?</label><br>
    <input type="radio" name="burger_name" value="1" checked> Already Devoured!<br>
    <input type="radio" name="burger_name" value="0"> New Option
  </div>

  <button type="submit">Add Burger</button>
</form>